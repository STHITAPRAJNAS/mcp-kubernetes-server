{
  "_comment": "IAM Trust Policy for MCP Kubernetes Server IRSA role.",
  "_usage": "Create an IAM role with this trust policy and attach the appropriate permissions policy.",
  "_commands": [
    "aws iam create-role --role-name mcp-kubernetes-server-role --assume-role-policy-document file://iam-role-trust-policy.json",
    "aws iam attach-role-policy --role-name mcp-kubernetes-server-role --policy-arn arn:aws:iam::aws:policy/AmazonEKSClusterPolicy",
    "aws iam put-role-policy --role-name mcp-kubernetes-server-role --policy-name eks-describe --policy-document file://iam-role-inline-policy.json"
  ],
  "Version": "2012-10-17",
  "Statement": [
    {
      "_comment": "Trust the EKS OIDC provider for the mcp-system namespace service account",
      "Effect": "Allow",
      "Principal": {
        "Federated": "arn:aws:iam::ACCOUNT_ID:oidc-provider/oidc.eks.REGION.amazonaws.com/id/OIDC_PROVIDER_ID"
      },
      "Action": "sts:AssumeRoleWithWebIdentity",
      "Condition": {
        "StringEquals": {
          "oidc.eks.REGION.amazonaws.com/id/OIDC_PROVIDER_ID:sub": "system:serviceaccount:mcp-system:mcp-kubernetes-server",
          "oidc.eks.REGION.amazonaws.com/id/OIDC_PROVIDER_ID:aud": "sts.amazonaws.com"
        }
      }
    }
  ]
}
